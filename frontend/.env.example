# Copy to .env.local for local overrides (not committed).
# Only VITE_* keys are exposed to the browser — never put secrets here.

# Optional: public app label / feature flags
# VITE_PUBLIC_APP_NAME=APIWatch

# Dev: use empty relative URLs for API (Vite proxy to Spring :8080). No base URL needed.
