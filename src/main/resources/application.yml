spring:
  application:
    name: APIMonitoring

  datasource:
    # Railway gives postgresql:// but Spring needs jdbc:postgresql://
    url: ${DATABASE_URL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update        # use 'update' not 'validate' until schema exists
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
    com.api.monitor: DEBUG