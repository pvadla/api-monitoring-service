# Local environment profile: activate with spring.profiles.active=local

spring:
  datasource:
    url: jdbc:h2:mem:apiwatch
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  config:
    import: optional:file:.env[.properties]
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

apiwatch:
  app:
    base-url: ${APIWATCH_BASE_URL:http://localhost:8080}
  mail:
    enabled: true
    from: ${APIWATCH_MAIL_FROM:no-reply@apiwatch.app}
  contact:
    to: ${APIWATCH_CONTACT_TO:support@apiwatch.app}

# Razorpay (use test keys in local; set RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET in production)
razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_SO1NqspUfKm94a}
  key-secret: ${RAZORPAY_KEY_SECRET:hm7n4x3lMCQcsXYuBBFTD82K}
  webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:}
  starter-amount-paise: ${RAZORPAY_STARTER_AMOUNT_PAISE:58000}
  plan:
    starter: ${RAZORPAY_PLAN_STARTER:plan_SOjYp6wfUgbCJv}
    pro: ${RAZORPAY_PLAN_PRO:missing}